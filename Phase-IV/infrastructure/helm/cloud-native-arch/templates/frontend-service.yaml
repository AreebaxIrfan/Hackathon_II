apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-native-arch.fullname" . }}-frontend-service
  labels:
    {{- include "cloud-native-arch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "cloud-native-arch.name" . }}-frontend